version: '3'
services: 
  app:
    build: .
    restart: always
    ports:
      - 5000:8080
  db:
    image: 'postgres'
    restart: always
    environment:
      - POSTGRES_PASSWORD=p
  adminer:
    image: adminer
    restart: always
    ports:
      - 5001:8080
